services:
  web:
    container_name: anibattle-web

    build:
      context: .
      args:
        MONGODB_URI: ${MONGODB_URI}

    env_file:
      - .env

    restart: unless-stopped

    ports:
      - "3000:3000"